import Vue from 'vue'
import vuex from 'vuex'
Vue.use(vuex);

export default new vuex.Store({
    state: {
        show: false
    },
    mutations: {
        increment(state) {
            state.show="515235416"
        }
    }
})
